---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: device-statistics
  namespace: test
  annotations:
    argocd.argoproj.io/sync-wave: "-300"
spec:
  refreshPolicy: CreatedOnce
  secretStoreRef:
    name: aws-cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: "device-statistics"
    creationPolicy: Owner
  data:
    - secretKey: IDP_PUBLIC_KEY
      remoteRef:
        key: "kubernetes/whitebox/device-statistics-cert-test"
  dataFrom:
    - extract:
        key: "kubernetes/whitebox/device-statistics-test"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: device-registration
  namespace: test
  annotations:
    argocd.argoproj.io/sync-wave: "-300"
spec:
  refreshPolicy: CreatedOnce
  secretStoreRef:
    name: aws-cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: "device-registration"
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: "kubernetes/whitebox/device-registration-test"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgresql
  namespace: test
  annotations:
    argocd.argoproj.io/sync-wave: "-300"
spec:
  refreshPolicy: CreatedOnce
  secretStoreRef:
    name: aws-cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: "postgresql"
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: "kubernetes/whitebox/postgresql-test"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: database-migrations
  namespace: test
  annotations:
    argocd.argoproj.io/sync-wave: "-300"
spec:
  refreshPolicy: CreatedOnce
  secretStoreRef:
    name: aws-cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: "database-migrations"
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: "kubernetes/whitebox/database-migrations-test"
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: postgresql
  namespace: test
  annotations:
    argocd.argoproj.io/sync-wave: "-300"
spec:
  refreshPolicy: CreatedOnce
  secretStoreRef:
    name: aws-cluster-secret-store
    kind: ClusterSecretStore
  target:
    name: "postgresql"
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: "kubernetes/whitebox/postgresql-test"
