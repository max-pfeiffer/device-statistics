replicaCount: 2

image:
    tag: "0.3.0"

service:
  port: 8000

livenessProbe:
  httpGet:
    path: /healthy
    port: 8000
readinessProbe:
  httpGet:
    path: /ready
    port: 8000

configMap:
    DATABASE_NAME: "devicestatistics"
    DATABASE_HOST: "postgresql"
    DATABASE_PORT: "5432"
    DATABASE_ALEMBIC_MIGRATION_ROLLBACK: "false"
    DATABASE_ALEMBIC_MIGRATION_REVISION: "INVALID"
    UVICORN_PORT: "8000"

secretRefName: "device-registration"
applicationType: "device-registration"