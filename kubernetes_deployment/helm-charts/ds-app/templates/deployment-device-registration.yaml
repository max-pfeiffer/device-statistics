apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.namePrefix }}-device-registration
  labels:
    app: device-registration
  annotations:
    argocd.argoproj.io/sync-wave: "0"
spec:
  replicas: {{ .Values.replicaCount.deviceStatistics }}
  selector:
    matchLabels:
      app: device-registration
  revisionHistoryLimit: 5
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: device-registration
    spec:
      containers:
        - name: device-registration
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: {{ .Values.service.port }}
          args: ["device-registration"]
          readinessProbe:
            httpGet:
              path: /healthy
              port: {{ .Values.service.port }}
          livenessProbe:
            httpGet:
              path: /ready
              port: {{ .Values.service.port }}
          envFrom:
            - configMapRef:
                name: {{ .Values.env.deviceRegistration.configMapRefName }}
            - secretRef:
                name: {{ .Values.env.deviceRegistration.secretRefName }}
          resources:
            {{- toYaml .Values.resources.apps | nindent 12 }}
          securityContext:
            {{- toYaml .Values.podSecurityContext | nindent 12 }}
          {{- with .Values.volumeMounts }}
          volumeMounts:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      {{- with .Values.volumes }}
      volumes:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
