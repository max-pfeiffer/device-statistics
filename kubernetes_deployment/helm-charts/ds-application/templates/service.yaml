apiVersion: v1
kind: Service
metadata:
  name: {{ include "ds-application.fullname" . }}
  labels:
    {{- include "ds-application.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ds-application.selectorLabels" . | nindent 4 }}
