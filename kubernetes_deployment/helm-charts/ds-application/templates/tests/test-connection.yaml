apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ds-application.fullname" . }}-test-connection"
  labels:
    {{- include "ds-application.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ds-application.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
